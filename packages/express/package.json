{
  "name": "@logscope/express",
  "version": "0.0.1",
  "description": "Express middleware for logscope — automatic request-scoped wide event logging.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Littletonconnor/logscope.git",
    "directory": "packages/express"
  },
  "keywords": [
    "logscope",
    "express",
    "middleware",
    "logging",
    "structured-logging",
    "wide-events",
    "request-logging"
  ],
  "author": "Connor Littleton",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --build",
    "clean": "rm -rf dist",
    "test": "node --experimental-strip-types --experimental-transform-types --test 'src/**/*.test.ts'",
    "test:watch": "node --watch --experimental-strip-types --experimental-transform-types --test 'src/**/*.test.ts'",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "peerDependencies": {
    "express": ">=4.0.0",
    "logscope": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "express": "^5.1.0",
    "logscope": "workspace:*",
    "tsdown": "^0.20.3"
  }
}
